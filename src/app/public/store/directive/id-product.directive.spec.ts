import { IdProductDirective } from './id-product.directive';

describe('IdProductDirective', () => {
  it('should create an instance', () => {
    const directive = new IdProductDirective();
    expect(directive).toBeTruthy();
  });
});
